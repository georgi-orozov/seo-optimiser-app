# PostgreSQL password
POSTGRES_PASSWORD=changeme

# Anthropic Claude API key
ANTHROPIC_API_KEY=sk-ant-api03-...

# Clerk: Backend JWT authority
CLERK_AUTHORITY=https://your-app.clerk.accounts.dev

# Clerk: Frontend publishable key
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me

# OpenTelemetry: OTLP endpoint for traces and logs.
# Already wired in docker-compose.yml pointing to the Aspire Dashboard — no need to set here for Docker.
# For local dotnet run: add "OpenTelemetry": { "OtlpEndpoint": "http://localhost:18889" }
# to backend/SEOOptimiser.API/appsettings.Development.json, then: docker compose up aspire-dashboard -d
